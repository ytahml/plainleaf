# Large document fixture

This file is intentionally modest in source control. Manual performance checks can duplicate the following paragraph when a larger sample is needed.

Plainleaf is a native Markdown editor and reader for technical writers. It keeps files portable, avoids hidden workspace metadata, and treats external changes as a conflict rather than an invitation to overwrite data.
